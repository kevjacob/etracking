# Supabase (optional). If set, app uses Supabase instead of localStorage.
# Do not commit .env — copy this file to .env and fill in values.

# --- Recommended: cloud-only (office + Vercel + mobile share one database) ---
# Same values as Vercel → Settings → Environment Variables.
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-public-key

# --- Legacy: local Docker (only if you still run npx supabase start) ---
# VITE_SUPABASE_URL=http://127.0.0.1:54321
# VITE_SUPABASE_ANON_KEY=<paste anon key from `npx supabase status`>

# --- Backup local → cloud (optional; not needed when everyone uses cloud above) ---
# Used by scripts/backup-to-remote.js and run-backup-daemon.js
# Local Docker: use service_role key so backup can read app_users (RLS blocks anon on that table).
# Get it with: npx supabase status -o env   then copy the value of SERVICE_ROLE_KEY= (starts with eyJ).
# Do NOT use PUBLISHABLE_KEY, SECRET_KEY, or S3_* keys—only the eyJ... JWT.
# SUPABASE_LOCAL_URL=http://127.0.0.1:54321
# SUPABASE_LOCAL_SERVICE_ROLE_KEY=<paste SERVICE_ROLE_KEY value from status -o env>
# (Or SUPABASE_LOCAL_ANON_KEY / VITE_SUPABASE_ANON_KEY if you don't need to backup app_users.)
# Online project (backup destination). Use service_role key from Dashboard > Settings > API for full write access.
# SUPABASE_REMOTE_URL=https://your-project.supabase.co
# SUPABASE_REMOTE_SERVICE_ROLE_KEY=<service_role secret from Dashboard>
#
# Backup daemon interval (milliseconds). Default 90000 = 90 seconds.
# BACKUP_INTERVAL_MS=90000
# Hourly: BACKUP_INTERVAL_MS=3600000
