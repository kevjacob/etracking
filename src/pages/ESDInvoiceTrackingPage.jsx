import InvoiceTrackingPage from './InvoiceTrackingPage'

/**
 * ESD Invoice Tracking – separate listing from Autocount.
 * Same setup (UI/workflow) but uses ESD invoice storage only.
 */
export default function ESDInvoiceTrackingPage() {
  return <InvoiceTrackingPage />
}
